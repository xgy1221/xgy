<view class="container">
  <view class="profile card">
    <view class="avatar">{{avatarText}}</view>
    <view>
      <view class="name">{{name}}</view>
      <view class="muted">{{phone}}</view>
      <view class="tag">{{roleName}}</view>
    </view>
  </view>

  <view class="section card" wx:if="{{multiRole}}">
    <view class="section-title">切换身份</view>
    <view class="muted tip">默认进入上次选择的角色，可随时切换。</view>
    <view
      class="role-item {{item.key === currentRoleKey ? 'on' : ''}}"
      wx:for="{{roleOptions}}"
      wx:key="key"
      data-role="{{item.key}}"
      bindtap="onPickRole"
    >
      <view>
        <view class="role-name">{{item.name}}</view>
        <view class="muted">{{item.desc}}</view>
      </view>
      <view class="tag {{item.key === currentRoleKey ? 'ok' : ''}}">
        {{item.key === currentRoleKey ? '当前' : '进入'}}
      </view>
    </view>
  </view>

  <view class="section card">
    <view class="section-title">绑定学员</view>
    <view class="list-item" wx:for="{{children}}" wx:key="id" data-id="{{item.id}}" bindtap="onSwitchChild">
      <view>
        <view class="role-name">{{item.studentName}}</view>
        <view class="muted">{{item.orgName || '机构'}} · {{item.grade || '未填年级'}}</view>
      </view>
      <view class="tag {{item.id === currentStudentId ? 'ok' : ''}}">
        {{item.id === currentStudentId ? '当前' : '切换'}}
      </view>
    </view>
    <view class="muted tip" wx:if="{{children.length}}">同一家长手机号可关联多个孩子，课表会随切换变化。</view>
    <view class="muted tip" wx:else>还没有学员档案，去添加孩子并选择教案。</view>
    <button class="btn-primary add-btn" bindtap="onAddChild">添加 / 完善学员</button>
  </view>

  <view class="section card">
    <view class="section-title">学习与活动</view>
    <view class="list-item" bindtap="goCourses">
      <text>我的教案 / 剩余课次</text>
      <text class="link">查看</text>
    </view>
    <view class="list-item" bindtap="goActivities">
      <text>比赛报名与往期赛果</text>
      <text class="link">进入</text>
    </view>
  </view>

  <view class="section card">
    <view class="list-item">
      <text>当前角色</text>
      <text>{{roleName}}</text>
    </view>
    <view class="list-item" bindtap="onLogout">
      <text>退出登录</text>
      <text class="danger">退出</text>
    </view>
  </view>
  <role-nav current="/pages/student/mine/mine" />
</view>
