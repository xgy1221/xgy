<view class="container">
  <view class="page-title">管理控制台</view>
  <view class="page-sub">全校运营快览 · 配置请用 Web</view>

  <view class="grid-2 section">
    <stat-card label="在读学员" value="{{stats.studentCount}}" />
    <stat-card label="教师" value="{{stats.teacherCount}}" />
    <stat-card label="开班中" value="{{stats.classCount}}" />
    <stat-card label="今日课次" value="{{stats.todayLessons}}" />
  </view>

  <view class="grid-2 section">
    <stat-card label="本月实收" value="¥{{stats.monthReceived}}" />
    <stat-card label="待收账款" value="¥{{stats.receivable}}" />
    <stat-card label="本月退费" value="¥{{stats.monthRefund}}" />
    <stat-card label="校区数" value="{{stats.openCampuses}}" />
  </view>

  <view class="section card">
    <view class="section-title">轻量入口</view>
    <view class="list-item" bindtap="go" data-url="/pages/admin/users/users">
      <text>账号与角色（只读列表）</text>
      <text class="link">查看</text>
    </view>
    <view class="list-item" bindtap="go" data-url="/pages/admin/org/org">
      <text>组织 / 校区</text>
      <text class="link">查看</text>
    </view>
    <view class="list-item" bindtap="go" data-url="/pages/admin/settings/settings">
      <text>常用开关</text>
      <text class="link">进入</text>
    </view>
  </view>

  <view class="web-hint">
    <view class="web-hint-title">管理员建议主力用 Web</view>
    <view class="web-hint-text">
      市面教培系统里，总管理员在电脑端做：全校区财务、教案定价、账号授权、校区配置。小程序给外出时瞟一眼在读规模与收款健康度；改数据、审批退费、改权限请打开 Web 管理端。
    </view>
  </view>

  <role-nav current="/pages/admin/home/home" />
</view>
